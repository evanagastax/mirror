/**
 * GitHub sync is handled entirely by GitHubSyncScreen.
 * Credentials are stored in AsyncStorage on-device only — never in Supabase.
 * This file is kept as a placeholder; actual sync logic lives in:
 *   src/services/githubSync.ts
 *   src/screens/GitHubSyncScreen.tsx
 */
export {};
